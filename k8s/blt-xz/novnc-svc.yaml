apiVersion: v1
kind: Service
metadata:
  name: novnc-service
  labels:
    app: novnc
  namespace: blt-xz
  annotations:
    external-dns.alpha.kubernetes.io/hostname: web.blt-xz.svc.technotut.net
spec:
  selector:
    app: novnc
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 6080